plugins {
    id 'application'
    id 'java'
}

group = 'com.vxl'
version = '1.0.0'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'io.netty:netty-all:4.2.2.Final'
    runtimeOnly 'org.slf4j:slf4j-simple:1.7.36'
    implementation 'com.mysql:mysql-connector-j:9.3.0'
    implementation 'com.zaxxer:HikariCP:6.3.0'
    implementation 'com.alibaba.fastjson2:fastjson2:2.0.57'
    implementation 'org.springframework.security:spring-security-crypto:7.1.0'
    implementation 'org.springframework:spring-core:7.0.8'
}

application {
    mainClass = 'com.vxl.VXLMayChu'
}

run {
    workingDir = projectDir
    jvmArgs '-Dfile.encoding=UTF-8', '-Dstdout.encoding=UTF-8', '-Dstderr.encoding=UTF-8', '-Xms512M', '-Xmx1024M'
}

sourceSets {
    main {
        java {
            srcDirs 'src'
        }
    }
}

jar {
    archiveFileName = 'mobiarmy3.jar'
    preserveFileTimestamps = false
    reproducibleFileOrder = true
    manifest {
        attributes 'Main-Class': 'com.vxl.VXLMayChu'
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    exclude 'META-INF/*.SF', 'META-INF/*.RSA', 'META-INF/*.DSA'
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
